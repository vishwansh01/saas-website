import React from "react";

const NotWork = () => {
  return (
    <div className="flex justify-center ali text-sm text-red-500">
      <div className="relative z-50 text-center m-8 w-96">
        Using Stripe Test mode because of Invite only issue of India on stripe
      </div>
    </div>
  );
};

export default NotWork;
